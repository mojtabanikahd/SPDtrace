# Generated by roxygen2: do not edit by hand

# Import Rcpp for C++ integration
importFrom(Rcpp, evalCpp)

# Import methods for S4 classes
importFrom(methods, new)

# Import stats for statistical functions
importFrom(stats, cor, fisher.test, p.adjust, median, rnorm, runif, sd)

# Import dplyr for data manipulation
importFrom(dplyr, "%>%", arrange, filter, group_by, mutate, select, summarize)

# Import stringr for string operations
importFrom(stringr, str_replace, str_split)

# Import ggplot2 for plotting
importFrom(ggplot2, aes, element_text, geom_line, geom_point, ggplot, 
          labs, scale_color_manual, theme, theme_minimal)

# Import ggpubr for publication-ready plots
importFrom(ggpubr, ggarrange, stat_compare_means)

# Import igraph for network analysis
importFrom(igraph, graph_from_adjacency_matrix, degree, betweenness, 
          closeness, page_rank, communities, cluster_louvain)



# Import rmatio for reading MATLAB files
importFrom(rmatio, read.mat)

# Import readxl for reading Excel files
importFrom(readxl, read_excel)

# Import Matrix for sparse matrix operations
importFrom(Matrix, Matrix, sparseMatrix)

# Export all functions
exportPattern("^[[:alpha:]]+")

# UseDynLib directive for C++ code
useDynLib(SPDtrace, .registration = TRUE)
